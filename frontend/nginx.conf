server {
    listen 5173;

    location / {
        root /usr/share/nginx/html;
        index index.html;

        # This is the critical line for React routing —
        # any route that doesn't match a real file falls back to index.html
        # so React Router can handle it client-side
        try_files $uri $uri/ /index.html;
    }
}